procedure main()

   every m := 2 to 5 do
      every k := 2 to 5 do
         write("m=", m, " k=", k, " n=",  compute(m, k))

end

procedure compute(m, k)

   return (2 ^ ((m ^ 2) - 3)) * (k ^ 3)

end
