System configuration:

	Linux (on all hardware platforms)

Latest Icon version:

	Version 9.5.0

Installer:

	Gregg Townsend
	Icon Project
	The University of Arizona

Missing features:

	None

Known bugs:

	None

Comments:

	Tested most recently on Ubuntu 12.10 (kernel 3.5.0).

	Ubuntu may require package installation in order to build:
	$ sudo apt-get install build-essential libx11-dev libxt-dev libxaw7-dev

Date:

	March 20, 2013
