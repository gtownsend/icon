System configuration:

	Linux (on all hardware platforms)

Latest Icon version:

	Version 9.5.25a of September 7, 2025

Installer:

	Gregg Townsend
	Icon Project
	The University of Arizona

Missing features:

	None

Known bugs:

	None

Comments:

	Tested on x64 Linux Mint 22.1 with cc 13.3.0 and Ubuntu Linux 6.8.0-62.
	Tested on aarch64 Raspberry Pi 5 with cc 12.2.0 and Linux 6.12.25.

	With graphics, additional packages may be needed:
	$ sudo apt install libx11-dev libxpm-dev libxt-dev libxaw7-dev

	Graphics users may also want to add some old-style fonts,
	because Icon can't use client-side font rendering:
	$ sudo apt install xfonts-100dpi  [or -75dpi, or both]


Date:

	September 7, 2025
