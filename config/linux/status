System configuration:

	Linux (on all hardware platforms)

Latest Icon version:

	Version 9.5.1

Installer:

	Gregg Townsend
	Icon Project
	The University of Arizona

Missing features:

	None

Known bugs:

	None

Comments:

	Tested on x86 Ubuntu 13.04 with gcc 4.7.3.
	Tested on x64 Xubuntu 13.04 with gcc 4.7.3.

	Ubuntu may require package installation in order to build:
	$ sudo apt-get install build-essential libx11-dev libxt-dev libxaw7-dev

	You may also want to add some old-style fonts, because Icon can't
	use client-side font rendering:
	$ sudo apt-get install xfonts-100dpi  [or -75dpi, or both]

Date:

	May 26, 2013
