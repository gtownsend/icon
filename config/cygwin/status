System configuration:

	Cygwin on Windows 7 using the X Window System for graphics

Latest Icon version:

	Version 9.5.1

Installer:

	Carl Sturtivant
	University of Minnesota

Missing features:

	None

Known bugs:

	Icon programs can only run from a Cygwin terminal.

	Graphics programs can only run if Cygwin/X is already running.

	The "options" test fails due to Unix dependencies.

Comments:

	See the special Cygwin page in the documentation directory.

	Tested on Cygwin-NT-6.1 with GCC 4.5.3.

Date:

	June 4, 2013
