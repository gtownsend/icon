#!/bin/sh
#
#  <ARIZONA-ONLY> <#%#%#%#%# TEMPORARY #%#%#%#%>

for d in *_*; do
   echo $d 1>&2
   cd $d
   (
   echo "#  Makefile definitions from config/unix/$d"
   cat <<'==EOF=='

#  CC       C compiler
#  CFLAGS   flags for building C files
#  LDRUN    flags for linking run-time system
#  MATH     math library to link with run-time system
#  RSWITCH  source code file of context switch code
#  XINCL    include path for graphics
#  XLINK    linking arguments for graphics
#  XPMDEFS  definitions for building XPM library

==EOF==
   grep CC= runtime.hdr
   grep CFLAGS rtt.hdr
   grep LDFLAGS runtime.hdr | sed s/LDFLAGS/LDRUN/
   grep LIBS runtime.hdr | sed s/LIBS/MATH/
   grep XINCL runtime.hdr
   grep XLIB xiconx.mak | sed s/XLIB/XLINK/
   grep XPMDEFS xpm.hdr
   grep RSWITCH= common.hdr
   ) | sed >Makedefs '
	s/+=/=/
	s/ *= */ = /
   ' >Makedefs
   cd ..
done
